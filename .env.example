# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Kopieren Sie diese Datei zu '.env' und passen Sie die Werte an Ihre Umgebung an.
# WICHTIG: Die '.env' Datei enthält sensible Daten und ist in .gitignore aufgeführt.
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Mögliche Werte: development | test | e2e | production
# Bestimmt welche Config-Datei aus config/ geladen wird
NODE_ENV=development

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port auf dem die Applikation läuft
PORT=3001

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Datenbank-Typ (postgres, mysql, sqlite, etc.)
DATABASE_TYPE=postgres

# Datenbank-Host (z.B. localhost oder IP-Adresse)
DATABASE_HOST=localhost

# Datenbank-Port (Standard PostgreSQL: 5432)
DATABASE_PORT=5432

# Datenbank-Benutzername
DATABASE_USERNAME=postgres

# Datenbank-Passwort
# ACHTUNG: Niemals echte Passwörter in diese Datei schreiben!
DATABASE_PASSWORD=your_secure_password_here

# Datenbank-Name
DATABASE_NAME=genericfoalts

# -----------------------------------------------------------------------------
# Database Connection Pool (Production)
# -----------------------------------------------------------------------------
# Maximale Anzahl von Verbindungen im Pool (Standard: 10)
# DATABASE_POOL_MAX=20

# Minimale Anzahl von Verbindungen im Pool (Standard: 2)
# DATABASE_POOL_MIN=5

# Timeout in Millisekunden für idle Verbindungen (Standard: 30000)
# DATABASE_POOL_IDLE_TIMEOUT=30000

# Timeout in Millisekunden für Verbindungsaufbau (Standard: 2000)
# DATABASE_POOL_ACQUIRE_TIMEOUT=60000

# -----------------------------------------------------------------------------
# Database SSL Configuration (Production)
# -----------------------------------------------------------------------------
# SSL-Verbindung aktivieren (true/false)
# DATABASE_SSL=true

# SSL-Zertifikat-Validierung (true=strict, false=selbstsignierte Zertifikate erlauben)
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# -----------------------------------------------------------------------------
# Database Advanced Settings
# -----------------------------------------------------------------------------
# Query-Caching aktivieren (true/false) - Standard: false
# DATABASE_CACHE_ENABLED=false

# Cache-Dauer in Millisekunden - Standard: 1000
# DATABASE_CACHE_DURATION=1000

# Schema-Synchronisation (true/false) 
# WARNUNG: NIEMALS in Production auf 'true' setzen!
# Wird automatisch durch NODE_ENV gesteuert
# DATABASE_SYNCHRONIZE=false
