{
  "database": {
    "database": "env(DATABASE_NAME_TEST)",
    "dropSchema": true,
    "synchronize": true,
    "pool": {
      "max": 5,
      "min": 1,
      "idleTimeout": 10000,
      "connectionTimeout": 2000
    }
  }
}
